# Archived v1: Directions

> 归档说明：新的研究主线位于 `tracks/`，产品型工作位于 `products/`。

这里保存研究方向地图和每条候选论文线的 scope 文档。

方向文档只描述：

- 应用场景；
- 用户/环境输入；
- Memory 与 Proactivity 分别承担什么；
- 可证伪 claim；
- 相关工作压力；
- 最小实验；
- 资源与数据阻塞项。

具体运行结果放入 `experiments/`，每周取舍放入 `weekly/`。
